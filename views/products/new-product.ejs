<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>After Dark - Crear producto</title>
    <link rel="preconnect" href="https://fonts.gstatic.com" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/general.css" />
    <link rel="stylesheet" href="/css/header.css" />
    <link rel="stylesheet" href="/css/footer.css" />
    <link rel="stylesheet" href="/css/forms.css" />

    <link rel="shortcut icon" href="/images/favicon.png" />
    <link
      href="https://use.fontawesome.com/releases/v5.0.10/css/all.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <%- include('../partials/header'); %>

    <main>
      <div class="form-container">
        <form
          class="form-auth"
          method="POST"
          action="/products"
          enctype="multipart/form-data"
        >
          <div class="form-title">
            <h2 class="titleLevelTwo">Crear producto</h2>
          </div>
          <div class="form-input">
            <label class="label" for="title">Nombre</label>
            <input
              name="title"
              type="text"
              id="title"
              class="form-auth-input"
              value="<%= locals.old && old.title %>"
            />
            <%if (locals.errors && errors.title) { %>
            <p class="form-error"><%= errors.title.msg %></p>
            <% } %>
          </div>

          <div class="form-input">
            <label class="label" for="description">Descripción</label>
            <input
              name="description"
              type="text"
              id="description"
              class="form-auth-input"
              value="<%= locals.old && old.description %>"
            />

            <%if (locals.errors && errors.description) { %>
            <p class="form-error"><%= errors.description.msg %></p>
            <% } %>
          </div>

          <div class="form-input">
            <label class="label" for="image">Imagen</label>
            <input
              type="file"
              name="image"
              id="image"
              class="form-auth-input"
              value="<%= locals.old && old.image %>"
              required
            />
            <%if (locals.errors && errors.image) { %>
            <p class="form-error"><%= errors.image.msg %></p>
            <% } %>
          </div>

          <div class="form-input">
            <label class="label" for="category">Categoría</label>
            <select
              name="category"
              id="category"
              class="form-auth-input"
            >
              <option default selected disabled>Seleccione una opción</option>
              <option value="VIEWING_GLASSES" <%= locals.old && old.category === "VIEWING_GLASSES" ? "selected" : "" %>>Lentes de sol</option>
              <option value="SUNGLASSES" <%= locals.old && old.category === "SUNGLASSES" ? "selected" : "" %>>Lentes de ver</option>
              <option value="ACCESORIES" <%= locals.old && old.category === "ACCESORIES" ? "selected" : "" %>>Accesorios</option>
            </select>
            <%if (locals.errors && errors.category) { %>
            <p class="form-error"><%= errors.category.msg %></p>
            <% } %>
          </div>

          <div class="form-input">
            <label class="label" for="colors">Colores</label>
            <select
              name="colors"
              id="colors"
              class="form-auth-input"
            >
              <option default selected disabled>Seleccione una opción</option>
              <option value="BLACK" <%= locals.old && old.colors === "BLACK" ? "selected" : "" %>>Negro</option>
              <option value="BROWN" <%= locals.old && old.colors === "BROWN" ? "selected" : "" %>>Marrón</option>
              <option value="SHIMMERING" <%= locals.old && old.colors === "SHIMMERING" ? "selected" : "" %>>Tornasolado</option>
              <option value="OTHER" <%= locals.old && old.colors === "OTHER" ? "selected" : "" %>>Otro</option>
            </select>
            <%if (locals.errors && errors.colors) { %>
            <p class="form-error"><%= errors.colors.msg %></p>
            <% } %>
          </div>

          <div class="form-input">
            <label class="label" for="price">Precio</label>
            <input
              type="number"
              id="price"
              class="form-auth-input"
              name="price"
              value="<%= locals.old && old.price %>"
            />
            <%if (locals.errors && errors.price) { %>
            <p class="form-error"><%= errors.price.msg %></p>
            <% } %>
          </div>

          <div class="form-input">
            <input type="submit" class="button-inverse" value="Aceptar" />
          </div>
        </form>
      </div>
    </main>

    <%- include('../partials/footer'); %>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW"
      crossorigin="anonymous"
    ></script>

    <script
      src="https://code.jquery.com/jquery-3.5.1.min.js"
      integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
      crossorigin="anonymous"
    ></script>

    <script src="/js/main.js"></script>
  </body>
</html>